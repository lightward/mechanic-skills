interface:
  display_name: "Mechanic CLI"
  short_description: "Use the Mechanic CLI to pull, preview, diff, check status, and publish task repos safely."
  default_prompt: "Use the Mechanic CLI workflow to inspect this task repo, check status, preview changes, show the diff, and publish only after confirmation."
